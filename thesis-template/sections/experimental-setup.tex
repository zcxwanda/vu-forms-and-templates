\section{Experimental Setup}\label{s:experimental-setup}
\yifan{RQs}
The experiment is organized around four research questions.

\parab{RQ1: Conversation quality.}
How does the presence of profile-based resistant clients influence overall conversation quality? Additionally, how does performance vary across different resistance categories?

\parab{RQ2: Diagnostic quality.}
How does the presence of profile-based resistant clients influence overall diagnostic quality? Additionally, how does performance vary across different resistance categories?

\parab{RQ3: Relationship between conversation and diagnosis.}
What is the association between conversation quality and diagnostic quality?

\parab{RQ4: Conversation characteristics.}
How does the presence of profile-based resistant clients influence conversation characteristics? Additionally, how do conversation characteristics vary across different resistance categories?

RQ1 and RQ2 use the existing DSM5AgentFlow evaluation outputs, and RQ3 links the results of the former two questions. RQ4 adds client-side and descriptive analysis because the original evaluator does not directly measure whether the simulated patient actually produced the intended resistant behavior.

\subsection{\yifan{Dataset and Materials}}
\paragraph{Dataset}
The dataset used in this thesis consists of generated DSM-5 assessment conversations. Each conversation is produced from a fixed set of experimental materials: a DSM5AgentFlow clinical profile, the DSM-5 Level 1 questionnaire, a client-resistance profile when applicable, and the final diagnosis generated by the diagnostic agent. The generated chat log, diagnosis, and metadata are then used as the unit of analysis. 

\paragraph{Clinical Profiles and Questionnaire.}
The experiments use five DSM5AgentFlow clinical profiles: depression, anxiety, post-traumatic stress disorder, bipolar disorder, and schizophrenia. These profiles define the target disorder and the clinical facts that ground the simulated patient's answers. They were selected because they are the main clinical profiles in the current implementation and cover different kinds of symptom presentations. The questionnaire material is the DSM-5 Level 1 cross-cutting symptom measure used by DSM5AgentFlow.


The resistance material consists of the taxonomy introduced earlier and fully reported in the Appendix~\ref{s:appendix-taxonomy}. Each resistant-client run uses one resistance sub-category and one intensity setting. For reframing-based resistance, the experiment also uses profile-specific truth maps, which indicate which questionnaire items are supported by each clinical profile.

In each run, the assistant asks the DSM-5 Level 1 questionnaire items in sequence, and the patient response is generated from the clinical profile, conversation history, and resistance profile when activated.

The study uses a controlled factorial design. Cooperative conversations use only the clinical profile to serve as the baseline. Resistant-client conversations use the same clinical profiles but add one resistance sub-category and one intensity setting. This makes it possible to compare cooperative and resistant conditions while keeping the underlying target disorder fixed. Intensity is defined at the conversation level: it controls the expected frequency of resistant turns rather than directly specifying the wording of each response. The turn-level procedure used to activate resistance and select expression levels is described in the following subsection.

The full experimental grid contains 925 generated conversations: $5$ clinical profiles $\times$ $5$ repeated runs $\times$ ($12$ resistance sub-categories $\times$ $3$ intensity settings $+$ $1$ cooperative baseline).

\begin{table}[t]
\centering
\caption{Number of generated conversations by condition.}
\label{tab:results-dataset-size}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lrr}
\toprule
Condition & Conversations & Notes \\
\midrule
Cooperative baseline & 25 & Five clinical profiles across repeated runs \\
Low resistance & 300 & Twelve resistance subcategories \\
Medium resistance & 300 & Twelve resistance subcategories \\
High resistance & 300 & Twelve resistance subcategories \\
\midrule
Total & 925 & -- \\
\bottomrule
\end{tabular}
}
\end{table}

\yifan{Refer to this writing method and order, but replace the content with yours:}
\yifan{\paragraph{Grounding Materials.}
To ensure consistent conversations across policy conditions, all sessions were guided by the same ten-item DSM-5-aligned questionnaire (Appendix~\ref{sec:appendix-questionnaire}). The Therapist Agent could flexibly rephrase, reorder, and extend the questions with context-dependent follow-ups while ensuring complete coverage. We also constructed a local DSM-5 knowledge base from APA disorder fact sheets \citep{apa2013dsm5factsheets}. Retrieved passages were used only to support the final diagnostic report, not resistance detection or therapeutic policy selection.}

\yifan{\paragraph{Simulated Conversation Dataset.}
We generated 800 therapist--client conversations for two experiments. For RQ2, we fixed the client profile to depression and evaluated six therapeutic policies under five resistance types (R0--R4), generating $5 \times 6 \times 10 = 300$ conversations. For RQ3, we fixed the adaptive policy and evaluated ten DSM-5 disorder profiles (Appendix~\ref{sec:clinical_profiles}) under the same five resistance types, generating $10 \times 5 \times 10 = 500$ conversations. 
The dataset contains an average of 18.1 dialogue turns.}

\yifan{\paragraph{Resistance Detection Dataset.}
We constructed an utterance-level dataset annotated with five resistance types (R0--R4) under a fixed depression profile to control for disorder-specific variation. The dataset contains 150 utterances: a 50-instance validation set (10 per type) for tuning the detector hyperparameters, evidence threshold $\tau$, and hostility-suppression rule, and a disjoint 100-instance test set (20 per type) for final evaluation. Further details are provided in Appendix~\ref{app:detector_data}.}

\subsection{Resistance Activation and Truth Maps}

Resistant runs use a turn-level activation mechanism. Before each patient response is generated, the system decides whether the selected resistance profile should be added to the patient prompt for that turn. If resistance is
not activated, the patient receives only the ordinary clinical-profile instruction. Because the resistance instruction is added only on activated turns, the resistance condition does not permanently replace the clinical patient profile. Non-activated turns are generated from the clinical profile alone, which helps preserve profile-grounded clinical information throughout the conversation.

The activation probability depends on the assigned intensity. Low, medium, and high intensity therefore control how often resistant behavior is expected to
appear across a conversation, rather than forcing every answer to be resistant. When a turn is activated, an expression level is also sampled to vary how strongly the selected resistance style is expressed in that response. The expression level is used as a generation device only; the main experimental condition remains the assigned resistance intensity. A cooldown mechanism is
used to reduce unnatural repetition by making it less likely that the same resistance instruction is applied too densely in adjacent turns.

Some resistance types require additional profile grounding. Reframing behaviors, including minimization, externalization, and denial, are only meaningful when the current questionnaire item refers to a symptom that should
be present in the assigned clinical profile. For these sub-categories, a profile-specific truth map is used to identify profile-positive questionnaire items. The truth map prevents the patient from denying, minimizing, or
explaining away symptoms that are not part of the case, which would create profile-ungrounded clinical content. As a result, reframing conditions can have a lower observed activation rate than other resistance categories: resistance is only applied on turns where the questionnaire item is eligible according to the truth map. Additional implementation materials, including resistance prompt-template summaries, truth-map examples, activation settings, and dialogue examples, are provided in Appendix~\ref{s:appendix-implementation-materials}.

\subsection{Conversation Logs and Metadata}

Each generated conversation is saved as a chat log. The log contains the assistant and patient turns, the clinical observation, the final diagnosis, and metadata describing the experimental condition. For resistant conversations, the log records the resistance dimension, category, sub-category, and intensity.

The log also stores turn-level generation metadata for patient responses, including whether resistance was activated and which expression level was used. These labels are stored for analysis only. They are not shown to the assistant agent during the conversation and are not used by the diagnostic agent when it generates the final diagnosis. This keeps the diagnostic task realistic: the
diagnostic agent must diagnose from the dialogue content rather than from hidden experimental labels.


\subsection{\yifan{Evaluation Metrics}}

The evaluation combines three types of measurements: the original
DSM5AgentFlow evaluator, client-side post-hoc metrics, and deterministic
conversation-characteristic features. Table~\ref{tab:metrics-rq-map}
summarizes how these metric groups are used for the research questions.

\begin{table*}[t]
    \centering
    \small
    \tabcap{Metric groups used in the experimental analysis.}
    \label{tab:metrics-rq-map}
    \begin{tabular}{p{0.22\textwidth} p{0.43\textwidth} p{0.25\textwidth}}
        \toprule
        \thead{Metric group} & \thead{Main measures} & \thead{Main use} \\
        \midrule
        Original conversation-quality metrics &
        DSM-5 coverage, clinical relevance, logical consistency, diagnostic justification, empathy/professionalism, average rubric score &
        RQ1 and RQ3 \\
        Original diagnostic-quality metrics &
        Diagnosis classification, text-match diagnosis accuracy, diagnostic justification, classification confidence &
        RQ2 and RQ3 \\
        Client-side post-hoc metrics &
        Resistance fidelity, question responsiveness, information yield, diagnostic certainty &
        RQ1, RQ2, and RQ4 \\
        Deterministic text features &
        Mean patient-answer length, activated-turn length, short-answer rate, nonresponse rate &
        RQ4 and qualitative interpretation \\
        \bottomrule
    \end{tabular}
\end{table*}

\subsubsection{Original DSM5AgentFlow Metrics}

The original evaluator is kept because it provides continuity with the upstream DSM5AgentFlow system. For conversation quality, the evaluator scores DSM-5 coverage, clinical relevance, logical consistency, diagnostic
justification, empathy/professionalism, and an average rubric score. These metrics mainly measure whether the conversation remains clinically coherent, professionally conducted, and useful for assessment.

For diagnostic quality, the evaluator reports the predicted diagnosis, diagnosis match, diagnosis classification, diagnostic justification, and
classification confidence. These metrics measure whether the final diagnostic output matches the expected clinical profile and whether the diagnostic explanation is plausible given the conversation. 

\subsubsection{Client-Side Post-Hoc Metrics}

The original evaluator mainly evaluates the assistant side and the final diagnosis. However, resistant-client simulation also needs to check whether the client behavior itself was successfully generated. For this reason, this thesis adds four client-side post-hoc metrics.

Resistance fidelity measures whether activated resistant turns match the target resistance sub-category. This metric is computed only for turns where resistance was activated. Question responsiveness measures how directly the patient answer responds to the assistant's current question. Information yield measures how much usable clinical information the patient answer provides,
including whether the answer contains enough relevant information for the assessment rather than only words or unimportant detail. Diagnostic certainty measures how confident the final diagnostic text appears.

Question responsiveness and information yield are computed both over all patient turns and over activated resistant turns. The activated-turn version shows the local effect of resistance, while the all-turn version shows the overall effect on the full conversation. This distinction is useful because a conversation may contain several resistant turns while still providing enough clinical information elsewhere for the diagnostic agent to make a correct diagnosis.

\subsubsection{Conversation Characteristics}

In addition to rubric-based and LLM-judged metrics, the analysis includes deterministic text features derived directly from the chat logs. These features include the mean length of patient answers, the mean length of activated resistant answers, the rate of very short answers, and the rate of nonresponses. These features are not written back into the original chat logs because they are post-hoc descriptive statistics rather than part of the generation process.

These descriptive features are mainly used for RQ4. They help identify surface differences between resistance categories, such as silence/nonresponse producing short or empty answers and irrelevant verbosity producing unusually
long answers. They are interpreted together with information yield because answer length alone does not indicate usefulness: a long answer may still be clinically unhelpful if it is off-topic or filled with irrelevant details.


\subsection{\yifan{Implementation Details}}
The final experiments use Gemini 2.5 Flash as the LLM backend for the assistant agent, the patient agent, and the diagnostic agent during dialogue generation. The original DSM5AgentFlow evaluator and the post-hoc client-side evaluator are also run with Gemini 2.5 Flash. Keeping the model fixed across cooperative and resistant conditions reduces the chance that observed differences are caused by different model backends rather than by the resistance condition.
%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../thesis"
%%% End:
